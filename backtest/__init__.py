# backtest package — Phase 6 implementation
"""
backtest — Phase 6 backtest engine and walk-forward evaluator.

Public modules:
- ``backtest.execution`` — Trade schema, fill model, fees+slippage
- ``backtest.runner`` — BacktestConfig, BacktestResult, run_backtest
- ``backtest.walkforward`` — WalkForwardConfig, run_walk_forward
"""
